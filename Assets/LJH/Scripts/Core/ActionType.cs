public enum ActionType
{
    Idle,
    MoveForward,
    MoveBack,
    MoveLeft,
    MoveRight,
    Attack,
    Defend,
    Dodge
}

public enum AgentState
{
    Idle,
    Moving,
    Attacking,
    Defending,
    Dodging,
    Dead
}